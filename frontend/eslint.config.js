module.exports = [
  {
    ignores: [".next/**", "out/**", "build/**", "next-env.d.ts"],
  },
  {
    // Extend next configs
    // Note: Flat config for next might require specific setup, 
    // but this is a starting point to avoid ESM errors.
  }
];
